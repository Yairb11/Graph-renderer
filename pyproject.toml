[project]
name = "graph-renderer"
version = "0.1.0"
description = "Graph renderer with 2 free variables xy"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.2",
    "flask>=3.1.3",
    "jinja2>=3.1.6",
    "uvicorn>=0.51.0",
]
